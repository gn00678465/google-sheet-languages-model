# Test fixtures

`service-account.json` holds a **throwaway RSA key generated locally for tests**. It has never been registered with Google and grants access to nothing. Do not replace it with a real key.
